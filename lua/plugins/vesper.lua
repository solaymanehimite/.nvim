return {
    "datsfilipe/vesper.nvim",
    name = "vesper",
    priority = 1000,
    config = function()
        -- require("vesper").setup({ transparent = true })
        -- vim.cmd("colorscheme vesper")
    end
}
